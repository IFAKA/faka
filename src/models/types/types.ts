export interface IImgs {
    growledge: string
    flowsh: string
    marz: string
}

export interface IProject {
    id: number
    href: string
    name: string
    desc: string,
    tags: string[]
}